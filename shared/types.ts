export type User = {
  userName: string;
  id: string;
};

export type Content = {
  user: string;
  text: string;
  image_url: string;
  tweet_url: string;
  created: string;
  profile_image_url: string;
};
